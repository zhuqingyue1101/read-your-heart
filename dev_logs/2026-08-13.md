# 开发日志 - 2026-08-13

## 完成事项
- [x] 上下文选项集对齐 PRD：关系阶段 8 选 / 目标 8 选 / 回复风格 8 选
- [x] 回复改写功能：结果页每条回复可改写（10 种方向），调用 /api/rewrite
- [x] crush 档案可视化：结果页「TA 的档案」+ 列表卡片展示最新状态/特征/风险
- [x] 骨架屏 loading：替换 emoji loading 为 shimmer 灰色骨架屏
- [x] 图片排序 + 超过5张提示：缩略图 ◀▶ 调序，超 5 张弹「一次最多看 5 张」
- [x] 历史记录详情页：Session 新增 records[] 存多轮记录，可展开详情 / 删除单条 / 继续读
- [x] 设置页：删除单个档案 / 清除全部数据（二次确认）
- [x] 气泡识别纠错：后端 /api/analyze 新增 swap_sides，前端结果页「切换双方重新分析」（覆盖上次记录）
- [x] 同步更新 docs/01-产品需求、02-技术方案、03-设计规范
- [x] 新增 docs/05-验收标准.md：按功能/非功能/通过门槛的验收清单
- [x] 批量 OCR 132 张样本，归纳 11 招式库（7 类）+ OCR 噪音问题，沉淀 docs/06-话术招式库.md
- [x] prompt 迭代：冷落/未回复场景分级（轻度给空间 vs 重度反讽夺权）+ OCR 噪音过滤规则
- [x] 移除回复改写功能（后端 /api/rewrite + 前端改写面板，同步文档）
- [x] 移除三指标进度条（心动信号/接球意愿/上头风险，同步文档）

## 待办事项
- [ ] 填入真实 DEEPSEEK_API_KEY 到 backend/.env
- [ ] 端到端验证新链路：新建→上传→结果→切换双方→历史→设置
- [ ] 验证 OCR 微信截图左右气泡标注准确率（气泡纠错的触发频率）

## 技术备注
- 后端 `/api/analyze` 新增 `swap_sides: bool = Form(False)`；`ocr_service.extract_text(image_bytes, swap_sides)` 交换左右标注
- localStorage key：`duxin_sessions`（每条含 records[]）、`duxin_session_counter`
- `types.ts` 新增 `AnalysisRecord` 接口；`Session.records` 存多轮分析记录（不存原始截图）
- store.ts 新增 `clearAllSessions()`、`deleteRecord(sessionId, recordId)`
- 气泡纠错复用时保留原图（App 内 `lastRequest`），重新分析并覆盖上一条记录而非新增
